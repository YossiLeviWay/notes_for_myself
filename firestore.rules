rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{allPaths=**} {
      allow read, write: if request.auth != null && (request.auth.uid == 'qelYRH3ns4daioIRieNXWU2hvpA2' || request.auth.token.email == 'admin@notes.com');
    }
  }
}
